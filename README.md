# Dead By Daylight Perk Browser

## Building
The package-win script gets finicky in windows. Try running `npm config set python <the path to python executable file>` if you run into cryptic GYP issues.